using UnityEngine;

public class closeEscp : MonoBehaviour
{

        public GameObject hideEspc;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
